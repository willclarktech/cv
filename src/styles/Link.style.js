import variables from './_variables.style.js'

const {
  mainTextColor,
  highlightColor,
} = variables

export const highlightStyle = {
  color: highlightColor,
}

export default {
  textDecoration: 'none',
  color: mainTextColor,
}
