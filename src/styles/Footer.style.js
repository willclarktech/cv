import variables from './_variables.style'
const { smallFontSize } = variables

export default {
  fontSize: smallFontSize,
  position: 'absolute',
  bottom: 0,
  textAlign: 'center',
  width: '100%',
}
