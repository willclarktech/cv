import variables from './_variables.style'

const {
  highlightColor,
  iconPad,
  iconWidth,
} = variables

export default {
  color: highlightColor,
  display: 'inline-block',
  paddingRight: iconPad,
  width: iconWidth,
  textAlign: 'center',
}
